import{EmbeddingBackfillStore}from'../src/embedding-backfill-store.js';import{OpenAICompatibleEmbeddingProvider}from'../src/openai-embedding-provider.js';
const required=(name:string)=>{const v=process.env[name];if(!v)throw new Error(`${name}_REQUIRED`);return v;};
const databaseUrl=required('DATABASE_URL'),profileId=required('EMBEDDING_PROFILE_ID'),providerName=process.env.EMBEDDING_PROVIDER??'openai',model=required('EMBEDDING_MODEL'),dimensions=Number(required('EMBEDDING_DIMENSIONS')),apiKey=required('EMBEDDING_API_KEY'),baseUrl=process.env.EMBEDDING_BASE_URL??'https://api.openai.com/v1',limit=Number(process.env.EMBEDDING_BACKFILL_LIMIT??'100');if(!Number.isInteger(dimensions)||dimensions<1)throw new Error('EMBEDDING_DIMENSIONS_INVALID');if(!Number.isInteger(limit)||limit<1||limit>1000)throw new Error('EMBEDDING_BACKFILL_LIMIT_INVALID');const provider=new OpenAICompatibleEmbeddingProvider(providerName,model,dimensions,apiKey,baseUrl),store=new EmbeddingBackfillStore(databaseUrl);try{const result=await store.backfill(profileId,provider,limit);console.log(JSON.stringify({profileId,provider:provider.provider,model:provider.model,dimensions:provider.dimensions,updated:result.updated,knowledgeIds:result.knowledgeIds},null,2));}finally{await store.close();}
