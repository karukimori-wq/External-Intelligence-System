export type AgingInput={confidence:number;lastSeenAt:Date|string;stage:string;contradictions:number;environmentChanged?:boolean};
export type AgingResult={effectiveConfidence:number;freshness:number;requiresRevalidation:boolean};
export function ageKnowledge(i:AgingInput,now=new Date()):AgingResult{const last=new Date(i.lastSeenAt).getTime(),days=Math.max(0,(now.getTime()-last)/86400000);const halfLife=i.stage==='promoted'?180:i.stage==='validated'?120:90;const freshness=Math.pow(.5,days/halfLife);const environmentPenalty=i.environmentChanged?.2:0;const contradictionPenalty=Math.min(.35,i.contradictions*.08);const effectiveConfidence=Math.max(0,Math.min(1,Number((i.confidence*(.55+.45*freshness)-environmentPenalty-contradictionPenalty).toFixed(4))));return{effectiveConfidence,freshness:Number(freshness.toFixed(4)),requiresRevalidation:!!i.environmentChanged||freshness<.6||i.contradictions>0};}
