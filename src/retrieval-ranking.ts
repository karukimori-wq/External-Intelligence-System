export type RetrievalSignals={textRank:number;confidence:number;projectsSeen:number;contradictions:number;stage:string;usefulnessScore?:number|null;freshness?:number|null;requiresRevalidation?:boolean};
export function retrievalScore(s:RetrievalSignals){const stageBonus=s.stage==='promoted'?.10:s.stage==='validated'?.06:0;const usefulness=s.usefulnessScore==null?0:Math.max(-1,Math.min(1,s.usefulnessScore))*.12;const freshness=s.freshness==null?0:Math.max(0,Math.min(1,s.freshness))*.08;const revalidationPenalty=s.requiresRevalidation?.06:0;return Number((s.textRank*.36+s.confidence*.26+Math.min(s.projectsSeen,5)*.03+stageBonus+usefulness+freshness-Math.min(s.contradictions,5)*.05-revalidationPenalty).toFixed(6));}
