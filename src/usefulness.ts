export type UsageSignals={retrievals:number;applications:number;verifiedSuccesses:number;verifiedFailures:number;avgIterationsSaved:number|null};
export type Usefulness={score:number;status:'unproven'|'helpful'|'strong'|'harmful';applicationRate:number;verificationRate:number};
export function calculateUsefulness(s:UsageSignals):Usefulness{if(s.retrievals<=0)return{score:0,status:'unproven',applicationRate:0,verificationRate:0};const applicationRate=s.applications/s.retrievals;const verifiedTotal=s.verifiedSuccesses+s.verifiedFailures;const verificationRate=verifiedTotal? s.verifiedSuccesses/verifiedTotal:0;const iterationBenefit=Math.max(-1,Math.min(1,(s.avgIterationsSaved??0)/5));const score=Math.max(-1,Math.min(1,Number((applicationRate*.2+verificationRate*.55+iterationBenefit*.25-(s.verifiedFailures>0?(s.verifiedFailures/Math.max(1,s.applications))*.35:0)).toFixed(4))));const status=score<0?'harmful':score>=.8?'strong':score>=.5?'helpful':'unproven';return{score,status,applicationRate:Number(applicationRate.toFixed(4)),verificationRate:Number(verificationRate.toFixed(4))};}
