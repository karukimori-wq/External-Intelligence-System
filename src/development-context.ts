import{IntelligenceStore}from'./store.js';import{DevelopmentCoordinationStore}from'./development-coordination.js';
export async function getDevelopmentContext(databaseUrl:string,input:{workspaceId:string;projectId:string;query:string;limit?:number}){const intelligence=new IntelligenceStore(databaseUrl),coordination=new DevelopmentCoordinationStore(databaseUrl);try{const[snapshot,items]=await Promise.all([coordination.getSnapshot(input.workspaceId,input.projectId),intelligence.retrieve({workspaceId:input.workspaceId,query:input.query,limit:input.limit??8,minConfidence:.35,includeDeprecated:false,trackUsage:false})]);return{workspaceId:input.workspaceId,projectId:input.projectId,snapshot,intelligence:items};}finally{await Promise.all([intelligence.close(),coordination.close()]);}}
