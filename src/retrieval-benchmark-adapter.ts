import{IntelligenceStore}from'./store.js';import{runRetrievalBenchmark}from'./retrieval-benchmark-runner.js';import{evaluateRetrieval}from'./retrieval-eval.js';import type{RetrievalEvalDataset}from'./retrieval-eval-dataset.js';
export async function benchmarkIntelligenceStore(databaseUrl:string,dataset:RetrievalEvalDataset){const store=new IntelligenceStore(databaseUrl);try{const results=await runRetrievalBenchmark(dataset,async({workspaceId,query,limit})=>store.retrieve({workspaceId,query,limit,minConfidence:.35,includeDeprecated:false,trackUsage:false}));const cases=dataset.cases.map(c=>({id:c.id,expectedKnowledgeIds:c.expectedKnowledgeIds,forbiddenKnowledgeIds:c.forbiddenKnowledgeIds}));return{results,metrics:evaluateRetrieval(cases,results)};}finally{await store.close();}}
