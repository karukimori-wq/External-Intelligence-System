import test from'node:test';import assert from'node:assert/strict';import{runRetrievalBenchmark}from'../src/retrieval-benchmark-runner.js';
const dataset={name:'x',version:'1',cases:[{id:'a',workspaceId:'w1',query:'cache',expectedKnowledgeIds:['11111111-1111-4111-8111-111111111111'],forbiddenKnowledgeIds:[],tags:[]},{id:'b',workspaceId:'w2',query:'retry',expectedKnowledgeIds:['22222222-2222-4222-8222-222222222222'],forbiddenKnowledgeIds:[],tags:[]}]};
test('runner preserves case isolation and captures ordered ids',async()=>{const calls:string[]=[];const r=await runRetrievalBenchmark(dataset,async i=>{calls.push(`${i.workspaceId}:${i.query}:${i.limit}`);return i.workspaceId==='w1'?[{id:'k1'},{id:'k2'}]:[{id:'k3'}];},3);assert.deepEqual(calls,['w1:cache:3','w2:retry:3']);assert.deepEqual(r.map(x=>x.returnedKnowledgeIds),[['k1','k2'],['k3']]);assert.ok(r.every(x=>x.latencyMs>=0));});
