import test from'node:test';import assert from'node:assert/strict';import{planEmbeddingBackfill,generateBackfillVectors}from'../src/embedding-backfill.js';
const base={kind:'pattern',conditions:{}};
test('backfill prioritizes promoted then validated then candidate',()=>{const r=planEmbeddingBackfill([{...base,id:'c',title:'c',statement:'c',stage:'candidate',confidence:.9},{...base,id:'p',title:'p',statement:'p',stage:'promoted',confidence:.8},{...base,id:'v',title:'v',statement:'v',stage:'validated',confidence:.7}], 'prod');assert.deepEqual(r.map(x=>x.id),['p','v','c']);});
test('knowledge already on target profile is skipped',()=>assert.equal(planEmbeddingBackfill([{...base,id:'p',title:'p',statement:'p',stage:'promoted',confidence:.9,embeddingProfileId:'prod'}],'prod').length,0));
test('generated vectors retain provider provenance',async()=>{const items=[{...base,id:'p',title:'P',statement:'S',stage:'promoted',confidence:.9}];const r=await generateBackfillVectors(items,{provider:'local',model:'m1',dimensions:2,async embed({texts}){assert.equal(texts.length,1);return{provider:'local',model:'m1',dimensions:2,vectors:[[1,0]]};}});assert.equal(r[0].provider,'local');assert.equal(r[0].dimensions,2);});
